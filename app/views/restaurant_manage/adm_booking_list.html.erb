<html>
<head>
  <style type="text/css">
      #customers
      {
          font-family:"Trebuchet MS", Arial, Helvetica, sans-serif;
          width:800px;
          border-collapse:collapse;
      }

      #customers td, #customers th
      {
          font-size:1em;
          border:1px solid #98bf21;
          padding:3px 7px 2px 7px;
      }

      #customers th
      {
          font-size:1.1em;
          text-align:left;
          padding-top:5px;
          padding-bottom:4px;
          background-color:#A7C942;
          color:#ffffff;
      }

      #customers tr.alt td
      {
          color:#000000;
          background-color:#EAF2D3;
      }
  </style>
</head>

<body>

<%= link_to '<<Go Home>>', action: 'adm_index', method: :get  %>   Top 100 reserve records.
<table id="customers">

  <tr >
    <th>ID</th>
    <th>User Name</th>
    <th>Restaurant </th>
    <th>Status</th>
    <th>Reserve time</th>
    <th>Updated at</th>
  </tr>

  <% @booking_list.each do |booker| %>
      <tr >
        <td><%= booker.id %></td>
        <td> <%= booker.name %>  </td>
        <td>
          <a href='<%=  APP_CONFIG['domain'] + booker.res_url.to_s  %>'><%= booker.restaurant_name %></a>
        </td>
        <td> <%= booker.status %>  </td>
        <td><%= booker.booking_time %> </td>
        <td> <%= booker.updated_at.to_date %></td>
      </tr>
  <% end %>
</table>

</body>
</html>